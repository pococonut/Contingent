version: "3"

services:
  backend:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
    container_name: backend
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    ports:
      - 8000:8000
    networks:
      - backend

networks:
  backend:
    external: true